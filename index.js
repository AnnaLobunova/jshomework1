"use strict"
const width=5;
const length=10;
const halfPerimeter=width+length;
const diff=length-width;
const square=width*length;
const times=length/width;
const exponent=Math.pow(length, width)
console.log(halfPerimeter, diff, square, times, exponent);